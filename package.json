{
  "name": "joke-generator",
  "version": "1.0.0",
  "description": "A full-stack joke generator web app",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "start": "concurrently \"npm start --prefix server\" \"npm start --prefix client\"",
    "test": "echo \"Tests not yet configured\" && exit 0"
  },
  "keywords": ["react", "express", "jokes", "full-stack"],
  "author": "apy65360-hash",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
